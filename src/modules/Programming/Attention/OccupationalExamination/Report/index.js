import { generateReportIndex } from "./EMO";

export function generateReportMain(lsDataReport = [], lsDataUser = [], resultExpoDLTD = []) {
    generateReportIndex(lsDataReport, lsDataUser, resultExpoDLTD);
}